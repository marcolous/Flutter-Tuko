// ignore_for_file: prefer_const_constructors, prefer_interpolation_to_compose_strings, avoid_print, must_be_immutable
import 'package:flutter/material.dart';

class Default {
  String? sound;
  String? image;
  String? textEN;
  String? textJP;
  Color? colorImage = Colors.white;
  Color? colorButton = Colors.white;


  Default({this.image, this.textEN, this.textJP, this.sound});
}
